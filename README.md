# endgateway.cc

Monorepo for the `endgateway.cc` stack.

## Layout

- `tunnel/` - Minecraft tunnel service for the VPS control plane and desktop agent.
- `desktop/` - desktop application placeholder.
- `web/` - web application placeholder.

The tunnel component is the only deployable service in this repository right now.
